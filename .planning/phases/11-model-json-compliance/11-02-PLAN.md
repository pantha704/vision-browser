# Phase 11, Plan 02: Model JSON Compliance Tests

## Goal
Write tests for the new JSON validation pipeline, retry with stricter prompts, and ModelResponseError.

## Requirements
- MODEL-03: Retry with progressively stricter prompts on JSON parse failure (max 2 retries)
- Verify 95%+ JSON compliance rate with mocked malformed responses

## Implementation

### Tests to add to tests/test_vision_client_mocks.py

1. **TestModelResponseError** - construction, context chaining, schema storage
2. **TestValidateJSONResponse** - valid JSON passes, prose raises ModelResponseError
3. **TestStricterPrompt** - progressively stricter messages on retry
4. **TestRetryWithStricterPrompt** - mock NIM returns prose, retry succeeds
5. **TestJSONComplianceRate** - run all mock responses through pipeline, assert 95%+ pass

## Verification
- All new tests pass
- All existing 169 tests still pass
- JSON compliance rate >= 95% with mock response suite
